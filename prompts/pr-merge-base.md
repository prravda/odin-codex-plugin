---
description: Merge PRs into the base branch with queue-like conflict resolution.
---
Smart Merge to Base Branch

Merge this single PR (or multiple PRs) into the base branch (usually main or master).
Merge them like a merge queue, and resolve conflicts intelligently when they occur.


$ARGUMENTS
